{
  "scripts": {
    "heroku-prebuild": "cd front && yarn install && yarn build && cd ../server && yarn install && yarn build",
    "start": "cd server && node dist/src/server.js"
  },
  "engines": {
    "yarn": "1.x"
  }
}
