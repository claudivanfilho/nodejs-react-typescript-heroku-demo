{
  "scripts": {
    "setup": "cd front && yarn install && cd ../server && yarn install",
    "heroku-prebuild": "cd front && yarn install && yarn build && cd ../server && yarn install && yarn build",
    "start": "cd server && node dist/src/server.js",
    "start-front": "cd front && yarn start",
    "start-server": "cd server && yarn dev",
    "dev": "concurrently --kill-others \"yarn start-front\" \"yarn start-server\""
  },
  "engines": {
    "yarn": "1.x",
    "node": "14.17.6"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
